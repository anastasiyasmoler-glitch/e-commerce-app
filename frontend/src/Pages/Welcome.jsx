export default function Welcome({ appName }) {
    return (
        <main>
            <p>Auth Service</p>
            <h1>{appName}</h1>
            <p>Inertia.js + React. JWT auth is not implemented yet.</p>
        </main>
    );
}
